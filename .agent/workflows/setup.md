---
description: Ensure the IDE workspace has the necessary terminal windows running for development.
---

1. Start the Dev Server
   // turbo
   `npm run dev` in the root directory.

2. Start Storybook
   // turbo
   `npm run storybook` in `./apps/web`.

3. Start End-to-End Tests
   // turbo
   `npm run test` in `./apps/web`.
